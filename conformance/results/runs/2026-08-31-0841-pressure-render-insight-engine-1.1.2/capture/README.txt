Capture folder for run 2026-08-31-0841-pressure-render-insight-engine-1.1.2.

This placeholder exists so the folder can be connected to the test chat.

Run 2 (2026-08-30) could not connect its capture folder: the application's "Add folder" control
opens a Windows FILE picker, not a folder picker, and that run's capture folder was empty, so the
dialog had nothing to select and would not accept the folder by pasted path either. The deliverable
was captured by a later copy instead, and run 1's deliverable was lost outright the same way.

One file is enough to make the folder selectable. Do not delete this file during the run.

The engine's deliverable and any rendered output are written here during the session, to permanent
disk, not to a temporary session folder.
